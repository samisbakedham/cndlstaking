"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var isClientSide_1 = require("./isClientSide");
var loaded = false;
function onWindowLoad() {
    return new Promise(function (resolve, reject) {
        if (!isClientSide_1.isClientSide()) {
            reject();
        }
        else if (loaded) {
            resolve();
        }
        else if (['loaded', 'interactive', 'complete'].indexOf(document.readyState) > -1) {
            loaded = true;
            resolve();
        }
        else {
            window.addEventListener('load', function () {
                loaded = true;
                resolve();
            }, false);
        }
    });
}
exports.onWindowLoad = onWindowLoad;
//# sourceMappingURL=onWindowLoad.js.map